list_a = [32, 12]
list_b = [4, 6, 31, 9]

# Lists concatination
list_a.extend(list_b) # [32, 12, 4, 6, 31, 9]

# Add to the list
list_x.append(12)

# Insert into a list
list_x.insert(idx, value)

# Remove an item from the list
list_x.pop(0) # Remove the first item
list_x.pop() # Remove the last item

# Slice the list
slice = list_x[2:3] # starting from idx 2 pick 3 elements (quantity)


